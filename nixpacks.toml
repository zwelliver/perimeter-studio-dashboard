[phases.setup]
nixPkgs = ['python313', 'nodejs_20']

[phases.build]
cmds = [
  'pip install -r requirements.txt',
  'cd studio-command-center/frontend && npm install && npm run build'
]

[start]
cmd = 'uvicorn app.main:app --host 0.0.0.0 --port $PORT'