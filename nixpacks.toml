[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x', 'python313']

[phases.build]
cmds = [
  'pip install -r requirements.txt',
  'cd frontend',
  'npm ci',
  'npm run build',
  'cd ..'
]

[start]
cmd = 'python3 main_complete.py'