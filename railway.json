{
  "deploy": {
    "buildCommand": "pip install -r requirements.txt && cd studio-command-center/frontend && npm install && npm run build",
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  },
  "volumes": [
    {
      "name": "reports",
      "mountPath": "/app/Reports"
    }
  ]
}